<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <style>
      h1 {
        text-align: center;
      }

      input {
        width: 100%;
        height: 25px;
        margin: 5px 0;
      }

      #submit {
        height: 40px;
        background-color: #01db1e;
        border: none;
      }

      .card {
        display: flex;
        flex-wrap: wrap;
        gap: 5px;
      }

      .product-card {
        border: 2px solid rgb(228, 217, 217);
        padding: 25px 25px;
      }
    </style>
  </head>
  <body>
    <%- include('partials/navbar') %>
    <div style="padding: 15px 400px">
      <h1>Home</h1>
      <form action="/products" method="get">
        <input type="text" name="tag" />
        <input type="submit" value="search" id="submit" />
      </form>
      <br />

      <div class="card">
        <% for (let i = 0; i < products.length; i++) {%>
        <div class="product-card">
          <img src="<%= products[i].image %>" width="100" height="100" />
          <h3><%= `${products[i].tag} ${products[i].brand}` %></h3>
          <p>Stock. <%= `${products[i].stock}` %></p>
          <p>Rp.<%= `${products[i].price}` %></p>
          <a href="/product/edit/<%= products[i].id %>">edit</a>
          <a href="/product/delete/<%= products[i].id %>">delete</a>
        </div>
        <%} %> <% if (products.length == 0) { %>
        <p>Products not found....</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
